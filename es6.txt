在es6里面{} 就是块级作用域  在块级作用域里面不可以提前未声明的变老了  这种情况叫做 暂时性死区

const 常量 只读属性 不能被修改    一但定义必须赋值 不能重复定义  不存在变量提升


array  省略逗号 嗝开


用等号给一个默认值 只有值绝对等于 ===   undefined的时候才会走默认值

用等号给一个默认值 只有值绝对等于 ===   undefined的时候才会走默认值

报错的情况  当右侧的值不是数组或者类数组的时候会报错

对象的结构赋值是根据 key  来一一对应的


当对象解构赋值的右侧不是对象的时候会调用对象的方法把右侧转换为对象


null 和 undefined  不能被转化为对象

let定义的属性不会挂载到window上

箭头函数 没有this this指向上级作用域

